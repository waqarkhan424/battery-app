import { Text, View } from 'react-native';

export default function DeviceScreen() {
    return (
        <View className="flex-1 items-center justify-center bg-slate-900">
            <Text className="text-cyan-400 text-xl font-bold">Device Screen</Text>
        </View>
    );
}
